// ============================================================
// search.js - AJAX Live Search
// ============================================================

$(document).ready(function () {

    // TODO: let searchTimeout = null; (debounce variable)

    // TODO: $('#search-input').on('input', function() {
    //     clearTimeout(searchTimeout);
    //     const q = $(this).val().trim();
    //     if (q.length < 2) { $('#search-results').hide().empty(); return; }
    //     searchTimeout = setTimeout(function() {
    //         $.ajax({
    //             url: BASE_URL + '/api/search.php',
    //             data: { q: q },
    //             success: function(data) { renderSearchResults(data); }
    //         });
    //     }, 300);
    // });

    // TODO: function renderSearchResults(data) {
    //     // Shfaq doctors dhe services si dropdown
    // }

    // TODO: Mbyll dropdown kur klikon jashtë
    // $(document).on('click', function(e) { ... });

});
